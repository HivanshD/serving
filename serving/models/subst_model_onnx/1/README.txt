# The actual model.onnx file is downloaded at container startup from
# models-proj01/production/subst_model_current.onnx — it is not committed
# to Git. See docker/Dockerfile.triton for the startup script.
