apiVersion: v1
kind: Service
metadata:
  name: mealie
  namespace: forkwise-app
spec:
  selector:
    app: mealie
  ports:
    - port: 9000
      targetPort: 9000
      nodePort: 30090
  type: NodePort
