apiVersion: v1
kind: Service
metadata:
  name: subst-serving
  namespace: staging-proj01
spec:
  selector:
    app: subst-serving
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 31082
  type: NodePort
