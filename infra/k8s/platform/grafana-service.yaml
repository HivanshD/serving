apiVersion: v1
kind: Service
metadata:
  name: grafana
  namespace: monitoring-proj01
spec:
  selector:
    app: grafana
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 31300
  type: NodePort
