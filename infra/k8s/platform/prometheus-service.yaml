apiVersion: v1
kind: Service
metadata:
  name: prometheus
  namespace: monitoring-proj01
spec:
  selector:
    app: prometheus
  ports:
    - port: 9090
      targetPort: 9090
      nodePort: 31090
  type: NodePort
